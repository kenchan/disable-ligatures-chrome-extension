import { defineConfig } from 'wxt';

export default defineConfig({
  manifest: {
    name: 'Disable Ligatures',
    description: 'Disable Ligatures on all websites',
    version: '0.2.0',
    homepage_url: 'https://github.com/kenchan/chrome-extension-disable-ligatures',
  },
});
